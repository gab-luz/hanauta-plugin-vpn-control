{
  "plugin_id": "vpn_control",
  "tasks": [
    {
      "id": "wireguard_cache",
      "enabled": true,
      "interval_seconds": 20,
      "timeout_seconds": 8,
      "working_dir": "${PLUGIN_DIR}",
      "command": [
        "python3",
        "${PLUGIN_DIR}/service_wireguard_cache.py"
      ]
    }
  ]
}
