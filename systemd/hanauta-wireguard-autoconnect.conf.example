# Which WireGuard interface (config) to bring up at boot.
# Example: WG_IFACE=myvpn uses /etc/wireguard/myvpn.conf
# Leave empty to disable autoconnect until interface is selected in plugin UI.
WG_IFACE=
